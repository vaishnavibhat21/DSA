#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
void readarray(int[],int);
void printarray(int[],int);
void pair_sum(int[],int);
int main()
{
    int N,s,i,j,k;
    printf("enter the number\n");
    scanf("%d",&s);
    scanf("%d",&N);
    for(i=1;N<=10000;i++)
    {
        int i,j;
        scanf("%d",&i);
        scanf("%d",&j);
    }
    for(j=1;k==3;j++)
{
  int sum;
        sum=i+j;
if(sum%k==0)
{
    printf("%d\n");
}
else
{
    printf("invalid output\n");
}
}
}
